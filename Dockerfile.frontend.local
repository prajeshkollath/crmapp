FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package.json yarn.lock ./

# Install dependencies (without frozen-lockfile for flexibility)
RUN yarn install

# Copy source code
COPY . .

EXPOSE 3000

CMD ["yarn", "start"]
